In this folder will be stored all the instances used in the last analysis described in "document.pdf", note that you need just the empty folder because the rest will be created using the two python files "setup.py" and "run.py"
